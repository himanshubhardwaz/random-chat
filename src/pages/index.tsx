import type { NextPage } from "next";

const Home: NextPage = () => {
  const joinRoom = () => {};
  return (
    <div>
      <button onClick={joinRoom}>Join Room</button>
    </div>
  );
};

export default Home;
